export enum Gender {
    MALE = 'MALE',
    FEMALE = 'FEMALE',
    OTHER = 'OTHER',
}

export enum UserRole {
    ADMIN = 'ADMIN',
    EMPLOYEE = 'EMPLOYEE',
    MANAGER = 'MANAGER',
}

export enum mediaType {
    IMAGE = 'iamge',
    VIDEO = 'video',
}
